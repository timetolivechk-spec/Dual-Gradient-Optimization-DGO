# Dual Gradient Optimization (DGO)  
**A Derivative-Free Global 1D Optimization Algorithm**

---

## 🎯 Описание  
Алгоритм **Dual Gradient Optimization (DGO)** разработан для поиска глобального минимума произвольной функции  
\[
f(x), \; x \in [x_{\min}, x_{\max}]
\]  
без использования производных.  
Предлагает простую, но эффективную стратегию сжатия интервала путём двух «агентов», оценивающих функцию по обе стороны от середины.  
Дополнительно алгоритм поддерживает multi-start запуск, чтобы минимизировать риск застревания в локальных минимумах.

---

## 📌 Особенности и новизна  
- Не требует вычисления градиентов или гессинов.  
- Использует адаптивное сужение интервала:  
  \[
    M_k = \frac{L_k + R_k}{2}, \quad \Delta_k = \frac{R_k - L_k}{4}
  \]  
  \[
    x_1 = M_k - \Delta_k,\; x_2 = M_k + \Delta_k
  \]  
  и сравнение \(f(x_1)\) и \(f(x_2)\).  
- Упрощённая реализация по сравнению с классическим методом золотого сечения, но с добавлением гибкости.  
- Multi-start режим (грубое сканирование + локальные DGO запуски) для повышения шансов нахождения глобального минимума.

---

## 🧮 Математическое описание алгоритма  

**Инициализация:**  
\[
L_0 = x_{\min}, \quad R_0 = x_{\max}, \quad \varepsilon = \text{допуск}, \; N_{\max} = \text{макс. итераций}
\]

**Итерация \(k\):**  
\[
M_k = \frac{L_k + R_k}{2}, \quad \Delta_k = \frac{R_k - L_k}{4}
\]  
\[
x_1 = M_k - \Delta_k,\; x_2 = M_k + \Delta_k
\]  
\[
f_1 = f(x_1),\; f_2 = f(x_2)
\]

**Обновление:**  
\[
\text{если } f_1 < f_2:
\quad (L_{k+1}, R_{k+1}) = (L_k, M_k)
\]
\[
\text{иначе: } (L_{k+1}, R_{k+1}) = (M_k, R_k)
\]

**Критерий останова:**  
\[
|R_{k+1} - L_{k+1}| < \varepsilon \;\; \text{или} \;\; k \ge N_{\max}
\]

**Результат:**  
\[
x^* = \frac{L_{k+1} + R_{k+1}}{2}, \quad f^* = f(x^*)
\]

---

## 🧩 Multi-start DGO  
1. Выполняется грубое сканирование \(x \in [x_{\min}, x_{\max}]\) с \(n\_points\) точками.  
2. Выбираются \(n\_best\) кандидатов с минимальными значениями функции.  
3. Для каждого кандидата запускается локальный DGO с интервалом \([c−s, c+s]\) (где \(c\) — кандидат, \(s\) — span).  
4. Из всех найденных минимумов выбирается глобальный.

---

## 💻 Установка и запуск  
```bash
# Склонируйте репозиторий
git clone https://github.com/timetolivechk-spec/Dual-Gradient-Optimization-DGO.git
cd Dual-Gradient-Optimization-DGO

# Убедитесь, что установлены зависимости (Python 3.x)
pip install numpy matplotlib

